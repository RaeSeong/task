1.접근방식
watcher server 종료 후 시도하는 재접속 시도를 수정합니다
 - 재접속 시도 기본값을 50ms로 설정하고 100*랜덤함수로 시간을 조율합니다.
 - 재접속 시도 횟수를 카운트 하여 10번까지는 랜덤 값으로 시도하도록 합니다.
 - else 구문으로 10번이 초과된 시도에 대해서는 10,000ms를 적용합니다.

2. 보완할 부분
 - 초기 재접속 시도 주기가 50ms로 설정되어 있는 것으로 판단하는 등 전반적인 코드 이해, 라이브러리 사용법을 제대로 알지 못 하는 상황입니다.